# Tree-Decision-Program
Creating a Decision Tree Program (decision tree) is a supervised learning method in the form of a hierarchical structure (tree) which is used for classification and regression by breaking down data into smaller subsets.
